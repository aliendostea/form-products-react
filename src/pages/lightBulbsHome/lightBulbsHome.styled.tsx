import styled from "styled-components";

export const BulbsHomeStyled = styled.div`
  display: grid;
  grid-template-columns: 1fr;
  align-content: start;
  font-size: 3rem;
  padding: 1rem 2rem 1rem 0;
`;
